(function(window, angular) {
    'use strict';

    var authenticationService = angular.module('Services');

    phonecatServices.factory('authentication', [
                                 function() {
                                     return;
                                 }
                             ]);
})(window, window.angular)